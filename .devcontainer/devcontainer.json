{
    "name": "root Dev Container",
    "image": "ubuntu:latest",
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "extensions": [
        "ms-vscode-remote.remote-containers"
    ],
    "runArgs": [
        "--cap-add=SYS_TIME"
    ],
    "postCreateCommand": "apt-get install -y cron sudo && service cron start && adduser --disabled-password --gecos '' root && adduser root sudo && echo '%sudo ALL=(ALL) NOPASSWD:ALL' >> /etc/sudoers && apt install systemctl -y",
    "remoteUser": "root"
}