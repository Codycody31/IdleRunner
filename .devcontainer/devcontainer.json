{
    "name": "IdleRunner - Dev Container",
    "image": "ubuntu:22.04",
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "extensions": [
        "ms-vscode-remote.remote-containers"
    ],
    "runArgs": [
        "--cap-add=SYS_TIME"
    ],
    "postCreateCommand": "apt-get update && apt-get install -y cron sudo && service cron start && apt install systemctl -y && adduser root sudo && echo '%sudo ALL=(ALL) NOPASSWD:ALL' >> /etc/sudoers",
    "remoteUser": "root"
}